import React from 'react';
import ReactDOM from 'react-dom/client';
import CollaboratoriLogin from './CollaboratoriLogin';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <CollaboratoriLogin />
  </React.StrictMode>
);
